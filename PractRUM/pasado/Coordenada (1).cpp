
#include "Coordenada.h"

/*bool operator != (tCoor c1, tCoor c2) {
	return !(c1 == c2);
}
bool operator == (tCoor c1, tCoor c2) {

	return (c1 == c2);
}
/*tCoor operator + (tCoor c1, tCoor c2) {//Este puede estar mal
	c1.fila + c2.fila && c1.columna + c2.columna;
}*/