#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	double x, y, resultado;
	cout << "valor de x: ";
	cin >> x;
	cout << "valor de y: ";
	cin >> y;
	resultado = (sqrt(pow(1.531, x + y)) + ((abs(pow(2.718, x) + pow(2.718, y)) * (sin(x) - tan(y))) /;
	(log10(y) * pow(3.141592, x));
	return 0;

}